# Base Ubuntu Development docker Image

This is a base Ubuntu 18.04 image already configured with a devuser.

You could run this image locally with:

`docker run -it antoniopicone/ubuntu_develop /bin/bash`